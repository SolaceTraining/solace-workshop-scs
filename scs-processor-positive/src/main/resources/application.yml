spring:
  cloud:
    stream:
      bindings:
        input:
          destination: POSITIVE.Q
          binder: local_solace
          group: Marc
        output:
          destination: T/tweets/stream/noyelling/positive/Marc
          binder: local_solace
      binders:
        local_solace:
          type: solace
          environment:
            spring:
              solace:
                host: tcp://localhost:55555
                msgVpn: default
                clientUsername: default
                clientPassword: default
                connectRetries: -1
                reconnectRetries: -1
      solace:
        bindings:
          input:
            consumer:
              queueAdditionalSubscriptions: T/tweets/stream/noyelling
logging:
  level:
    root: ERROR
    com.solacesystems.jcsmp: INFO
    org.springframework: INFO
    
server:
  port: 8091