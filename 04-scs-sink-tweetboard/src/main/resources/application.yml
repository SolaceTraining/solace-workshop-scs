spring:
  cloud:
    stream:
      bindings:
        input:
          destination: TWEETS.Q.BOARD.ATTENDEE
          binder: solaceBroker
          # Note that no "group" is specified so this input will follow the "publish-subscribe" pattern
      binders:
        solaceBroker:
          type: solace
          environment:
            solace:
              java:
                host: tcp://localhost:55555
                msgVpn: default
                clientUsername: default
                clientPassword: default
                connectRetries: 3
                connectRetriesPerHost: 0
                reconnectRetries: 3
      solace:
        bindings:
          input:
            consumer:
              queueAdditionalSubscriptions: ATTENDEE/tweets/stream/nofeatures/noyelling/positive
logging:
  level:
    root: INFO
    com.solacesystems.jcsmp: INFO
    org.springframework: INFO

server:
  port: 8094
